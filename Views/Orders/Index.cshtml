@model IEnumerable<PC_Spiele.Models.Order>

@{
    ViewData["Title"] = "Index";
    bool isAdmin = User.IsInRole("Admin");
}

<h1>Index</h1>

@if (isAdmin)
{
    <p>
        <a class="btn btn-primary" asp-action="Create">Create New</a>
    </p>
}
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.user)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.user.UserName)
            </td>
            <td>
                    <a class="btn btn-primary" asp-action="Details" asp-route-id="@item.Id">Details</a>
                    @if (isAdmin)
                    {
                        <a class="btn" style="background-color: gray; color: white" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                        <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                    }
                <a class="btn btn-primary" style="background-color: green; color: white" asp-action="Invoice" asp-route-id="@item.Id">Invoice</a>
            </td>
        </tr>
}
    </tbody>
</table>
